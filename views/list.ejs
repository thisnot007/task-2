<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TODO App</title>
    <link rel="icon" href="data:,">
    <link rel="stylesheet" href="css/styles.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Mulish:ital,wght@0,200;1,200&display=swap');
        </style>
</head>
<body>
    <h1 style="color: rgb(109, 17, 143)" align="center">TODO App</h1>
    <div class="box" id="heading">
        <h2><%= listTitle %></h2>
    </div>
    <div class="box">
        <div class="item shift" ><h3>👉Welcome to TODO app!</h3></div>
        <div class="item shift" ><h3>👉Write a task & hit the + button to add it.</h3></div>
        <div class="item shift" id="space"><h3>👉After completing the task, check the box to delete it.</h3></div>
        <% NewListItems.forEach(function(i){ %>
        <form action="/deletelist" method="post">
            <div class="item">
                <input type="checkbox" name="checkBox" value="<%= i._id %>" onChange="this.form.submit()">
                <p><%= i.name %></p>
            </div>
            <input type="hidden" name="listName" value="<%= listTitle %>"></input>
        </form>
        <% }) %>

        <form class="item" action="/list" method="post">
            <input type="text" name="newitem" placeholder="New Task" autocomplete="off">
            <button type="submit" name="list" value="<%= listTitle %>"> +</button>
        </form>
    </div>
    
</body>
<footer>
    ©️ Divyanshi
</footer>
</html>
